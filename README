prj1 单个文件夹下，指定源文件进行编译，更改头文件后会重新编译
prj2 单个文件夹下，编译当前文件夹下的所有c文件，更改头文件后会重新编译
prj3 单个文件夹下，编译静态库文件
prj4 单个文件夹下，编译静态库文件，obj文件放入obj文件，d文件放入dep文件夹，静态库文件放入lib文件夹
prj5 单个文件夹下，编译动态库文件
prj6 单个文件夹下，编译动态库文件，obj文件放入obj文件，d文件放入dep文件夹，动态库文件放入lib文件夹
prj7 子目录编译，包括链接静态库，动态库
